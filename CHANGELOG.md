# 更新日志

本文件记录 LogVault 的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.3.5] - 2026-09-03

「下面的都被截断了」。2.3.4 给「立即清理」补的那张「维护结果」卡片，在 1622×872 的窗口里根本滚不到——问题不在卡片，而在承载它的面板。`.lv-panel` 是列向 flex 容器，子项默认 `flex-shrink:1`，而 `.lv-card` 带 `overflow:hidden`；按 CSS 规范，`overflow` 不为 `visible` 会把子项的自动最小尺寸从 min-content 高度解析为 0。于是卡片被压到远小于自身内容的高度、裁掉超出的部分，面板的 `scrollHeight` 始终等于 `clientHeight`，`overflow:auto` 便永远不生成滚动条：内容既没显示出来，也没溢出到可以滚动。上一版「点清理一直是 0」的追问也源于此——解释就写在这张看不见的卡片里。

### 修复

- **总览页底部无法滚动到，卡片裁掉了自己的内容**。新增 `.lv-panel > *{flex:0 0 auto;}`，让面板的直接子项按内容高度占位、不再参与收缩，超出的部分交给面板滚动。需要内部滚动的三处布局（`.lv-stream` 实时日志、`.lv-split` 文件浏览、`.lv-results` 搜索结果）本来就在文件更靠后的位置重新声明了 `flex:1`，特异性相同而顺序在后，因此仍然铺满面板并各自滚动。实测六个页签的所有卡片裁切量归零，总览面板 `scrollHeight` 1037 / `clientHeight` 754，底部可达。
- **手动清理的报告答在屏幕外**。「立即清理」在页头，报告卡片在可滚动面板的最末一块，两者不同屏时只能看到 toast 里的三个数字。现在渲染完报告会调用 `scrollIntoView({block:"nearest"})`，已在视野内时不动，不在时最小幅度滚到可见。
- **`.log.active` 回退流曾被标成可删除**。`_prepare_log_path` 在规范路径被过期目录占用时会退到 `<名称>.log.active` 继续写入，但「是否正在写入」这条判断在清理器、压缩器和文件列表里各写了一份，文件列表那份只认 `.log`，于是这个仍被 handler 持有的文件在 WebUI 里带上了可删除标记。三处统一到 `log_handler.is_active_log_name()`；删除端点原本也只挡 `.log`，同样换成共享判定（POSIX 上删除已打开的文件会成功，handler 继续往一个已被解链的 inode 写，之后所有记录静默消失）。

### 新增

- **清空全部日志**。页头新增红色「清空日志」按钮，走新的 `purge_logs` 端点调用 `LogCleaner.purge_all()`：忽略压缩延迟、保留天数与总量上限，删除数据目录下所有已关闭的日志（含 `.gz` 归档）。正在写入的流（`.log` / `.log.active`）按 `is_active_log_name()` 保留，导出包也不动——那是下载产物而非日志，导出中心已有自己的清空入口。此前想清空目录得先改三项配置、跑两趟清理，或者在文件列表里勾几百行。
- **清空前预告规模**。确认弹层用文件列表已有的 `deletable` 标记（与清理器同一条规则算出）报出即将删除的文件数与体积，并说明哪些不会被删。这是快照因此仅供参考，服务端删除前会重新扫描与 stat。
- **清空报告复用维护结果卡片**。`purge_all()` 返回的结构与 `cleanup()` 一致，但携带 `mode:"purge"` 且刻意不带 `thresholds`：这一趟没有遵守任何阈值，报出配置值会误导。卡片据此把「生效阈值」一行换成「执行方式」，并省掉恒为 0 的压缩计数；一个文件都没删时给出说明——这通常意味着当前只有正在写入的日志。
- **五个新测试**（总数 106 → 111）。清空全部：所有已关闭文件被删而两种活动流与导出目录（包括名字里带 `.log.` 因而会被名称过滤器认成日志的导出文本）完好、报告字段与释放字节数逐项核对且确认没有 `thresholds`；关闭压缩与清理并把保留天数设为 3650 天时，例行清理删 0 个而清空仍删 1 个；空目录报全 0 而不是报错。WebUI 侧断言 `.log.active` 在文件列表里 `active=true`、`deletable=false` 且删除端点会拒绝它。资产侧断言按钮 id、前端调用、路由注册与 `purge_all()` 四处名字一致——只改其中一处会留下一个看起来能用、实际什么都不做的控件。

### 变更

- **危险操作的守卫改为按端点核对**。此前测试硬编码「确认弹层出现 3 次」，加一个危险按钮就会失败但看不出原因。现在改为统计 `delete` / `clean` / `purge_logs` / `export_purge` 四个变更类端点的调用数并要求与确认弹层数量相等：新增一个未加确认的变更端点会在这里失败。
- **维护入口整体互锁**。「立即清理」「立即压缩」「清空日志」三个按钮在任一趟处理期间一起禁用，避免两趟处理争抢同一批文件。
- **WebUI 接口 17 → 18**（采集诊断页的计数随之变化）。

## [2.3.4] - 2026-09-03

WebUI 点「立即清理」永远返回「压缩 0 · 已删除 0 · 释放 0 B」。清理链路本身没坏：默认阈值下压缩要等文件满 1 天、删除要等满 30 天或总量超过 500 MB，192 MB 的新日志一条都不够条件，而界面只报三个 0，没法自证。顺着这条线还查出一个更严重的问题——轮换归档一直在互相覆盖。

### 修复

- **轮换归档互相覆盖，历史归档被静默销毁**。`CompressedRotatingFileHandler.doRollover` 把 `all.log.5` 压成同名的 `all.log.5.gz`，但槽位号每次轮换都会被回收，于是下一轮产生的 `all.log.5.gz` 通过 `os.replace` 直接盖掉上一代归档；清理器压缩中间槽位时同理。结果是「已压缩」长期停在个位数、磁盘上永远只剩最后一代归档，这也是此前「日志不全」的残留根因。现在归档名按源文件的 mtime 打时间戳（`all.log.20260903-024011.gz`），同秒冲突再追加 `-1`/`-2` 计数器；按时间轮换本身产生的唯一名（`all.log.2026-09-01`）保持原样。读取路径一直用 `gzip.open` 透明解压，改名不影响查看、搜索与导出。
- **`compression_after_days: 0`（不延迟）可能什么都不压**。压缩阈值此前统一走 `mtime < now - N 天` 的比较，`N` 为 0 时阈值就是「此刻」，而文件系统的时间戳粒度（NTFS 会向上取整，FAT 是 2 秒）可以让刚落地的 mtime 不小于这个阈值，于是文件被判为「还太新」。现在阈值为 0 时直接跳过时间比较，只保留「文件已关闭」这一条判断，新增的强制压缩也走同一条路径。

### 新增

- **清理结果可解释**。`cleanup()` 除动作计数外还返回本次的扫描快照：`scanned` / `total_bytes`、`skipped.{active, already_compressed, too_new}`、`oldest_age_hours`、`next_compress_in_hours`，以及真正生效的 `thresholds`（压缩延迟 / 保留天数 / 总量上限，对应功能关闭时为 `null`）。快照在压缩与删除两趟处理之前采集，因此报告描述的正是阈值当时面对的状态。
- **总览页「维护结果」卡片**。手动清理后自动切回总览并展示这份报告：本次动作、扫描范围、跳过明细（写入中 / 已归档 / 未到压缩时间）、生效阈值、下一批压缩倒计时、最旧归档年龄、被回收的导出包数量。零动作时补一句说明，明确这属于正常状态而不是故障。
- **「立即压缩 N 个轮换日志」**。报告中 `too_new > 0` 时，卡片会给出这个按钮：走 `clean` 端点的 `deep=true`，本次忽略压缩延迟，把所有已关闭的轮换文件立刻归档。正在写入的日志始终不动，配置不会被修改，下一次定时清理仍按原来的延迟执行。
- **八个新测试**（总数 98 → 106）。归档保留：连续轮换保留每一代归档、清理器与 handler 的归档不再互相覆盖、同秒归档追加计数器、按时间轮换的文件名保持自己的后缀；清理报告：零动作时报告完整解释原因与倒计时、功能关闭时阈值报 `null`；强制压缩：`deep` 会归档当天刚轮换的文件且报告仍显示配置里的延迟、正在写入的日志不受影响。

### 变更

- **手动清理的反馈**。零结果时 toast 追加「无需清理，原因见总览」并自动切到总览页展示报告；清理期间「立即清理」与「立即压缩」两个入口一起禁用，避免同一趟处理被重复触发。

## [2.3.3] - 2026-09-03

WebUI 的「立即清理」点下去没有任何反应。根因不是 AstrBot 的确认弹窗跟别人不一样，而是插件页被放在一个缺少 `allow-modals` 的 iframe sandbox 里，浏览器把原生确认框静默拦截了。

### 修复

- **三个危险操作的按钮点了没反应**。AstrBot Dashboard 用 `sandbox="allow-scripts allow-forms allow-downloads"` 的 iframe 承载插件页，其中没有 `allow-modals`，因此 `window.confirm()` 会被浏览器静默拦截并直接返回 `false`。「立即清理」「删除所选日志」「清空导出包」三处都是 `if (!确认) return;` 的写法，于是点下去既不弹窗也不报错，看起来就像按钮坏了。现在改为页内自绘的确认弹层，不再依赖浏览器原生对话框。
- **皮肤 / 密度 / 页签偏好刷新即丢失**。同一个 sandbox 也没给 `allow-same-origin`，插件页运行在 opaque origin 下，`localStorage` 读写会抛 `SecurityError`。六套皮肤的选择从上线起就没有被记住过，而异常被 try/catch 吞掉了，所以连报错痕迹都没有。现在偏好改走插件自己的 Web API 落到 `data/console_prefs.json`，内存缓存与 `localStorage` 只在可用时充当加速层。

### 新增

- **页内确认弹层**。带三角警告图标、可选补充说明与红色实体主按钮。打开时焦点落在主按钮上并在弹层内做 Tab 循环，Escape、点击遮罩、取消按钮都会关闭弹层并把焦点还给触发它的按钮，取消路径不会发出任何请求。六套皮肤与两档密度下自适应（玻璃荧光额外加背景模糊），窄屏改为纵向铺满按钮，`prefers-reduced-motion` 下不做入场动画。
- **控制台偏好端点**。新增 `prefs`（GET）与 `prefs_save`（POST），按白名单只接受 skin / density / tab 三个键，非法值直接丢弃；写入先写临时文件再用 `os.replace` 原子替换，避免刷新时读到半个文件。前端做 400ms 防抖并对内容指纹去重，连续切换皮肤只会落盘一次。
- **六个新测试**（总数 92 → 98）。前端侧断言源码中不出现 `window.confirm` / `alert` / `prompt`、弹层标记具备 `role="dialog"` 与 aria 关联、偏好读写不依赖 `localStorage`；后端侧断言非法偏好被过滤、合并写入后不留 `.tmp` 残留、偏好文件损坏时回落为空配置。

### 变更

- **剪贴板兜底加固**。opaque origin 下 `navigator.clipboard` 会直接 reject，`execCommand` 兜底路径补上离屏定位、`focus()` 与 `setSelectionRange`，修正部分浏览器下复制出空内容的问题。
## [2.3.2] - 2026-08-29

2.3.1 的 logo 只进了 `assets/`，AstrBot 插件管理页仍然显示占位图标。

### 修复

- **插件管理页不显示图标**。AstrBot 只从插件根目录的 `logo.png` 读取图标（`star_manager` 里硬编码的文件名，`metadata.yaml` 没有对应字段），2.3.1 把资源全放进了 `assets/`，因此卡片一直回落到默认占位图。现补上根目录 `logo.png`（512×512）。

### 新增

- **满幅方形图标源**。`assets/logo-square.svg` 是不带圆角的满幅变体，根目录 `logo.png` 由它渲染。Dashboard 会自己给插件图标加圆角裁切，图片自带圆角会造成二次裁切，因此这一档刻意不做圆角；带圆角的 `logo-badge.svg` 继续用于 README 与社交预览。
- **图标一致性测试**。断言根目录 `logo.png` 存在、是 512×512 PNG，且 `logo-square.svg` 与 `logo.svg` 的三段路径一致、不含圆角。

## [2.3.1] - 2026-08-29

本次发布只做品牌资源，不改动任何功能逻辑。

### 新增

- **插件 Logo**。`assets/logo.svg` 是 24×24 单色矢量标记，采用「日志页 + 盾牌底」的轮廓：上半是折角文档（承接原 header 图标的语义，对应 Log），下半收成盾牌尖（对应 Vault）。描边使用 `currentColor`，放进任何皮肤都会跟随强调色，20px 下仍可辨识。
- **渐变徽章与光栅图**。`assets/logo-badge.svg` 是 512×512 深蓝底渐变徽章，描边走蓝（`#4da3ff`）到青（`#37f5d4`）的渐变，呼应深空控制台与玻璃荧光两套皮肤的强调色；同时提供 `logo-512.png`、`logo-256.png`、`logo-128.png`、`logo-32.png` 四档光栅图。
- **社交预览图**。`assets/social-preview.png`（1280×640）用于 GitHub 仓库的 Social preview，需要在仓库 Settings → General 手动上传。
- **资源一致性测试**。新增测试断言 `assets/logo.svg` 使用 `currentColor`、WebUI header 内联的三段路径与 `assets/logo.svg` 完全一致、四档 PNG 与社交预览图存在，避免图标出现两份互相漂移的定义。

### 变更

- **WebUI 左上角品牌标记**换成新的 logo 标记，仍是内联 SVG（Page 静态资源不能跨目录引用），仍用 `currentColor` 跟随皮肤强调色。
- **README 顶部**加入居中 logo。

### 修复

- **README 文档过期**。WebUI 章节仍写着「五个页签」且把打包下载算在运行总览里，实际 2.3.0 起导出已是独立的第六个页签；同步补上导出中心的范围、时间窗、级别与关键词、双格式与导出历史说明。

## [2.3.0] - 2026-08-28

本次发布把导出从总览页的一张卡片扩展为独立的导出中心，并修复了两个此前未被发现的真实缺陷。

### 新增

- **导出中心独立页签**。导出成为第六个页签（图标 `⤓`），左栏是导出构建器，右栏是导出历史，窄屏下自动折叠为单栏。
- **四种导出范围**。预设（全部日志 / 仅错误 / 核心日志）、当前分类、日志文件页已勾选的文件、指定插件。切换范围时下方会说明这次到底会打包什么，勾选数量为 0 时相关按钮保持禁用并给出提示。
- **时间窗过滤**。可填「最近 N 天」，也可填起止日期（`YYYY-MM-DD`，支持只填一端）。填 0 或留空表示不限时间。单条日志按记录时间逐行裁剪，跨天的文件只会带出窗口内的行。
- **级别与关键词过滤**。五个级别以 chip 形式多选（DEBUG / INFO / WARNING / ERROR / CRITICAL），另可叠加关键词，两者同时生效。
- **双格式输出**。`ZIP 压缩包`保留原有目录结构，`合并 TXT`把命中的行按时间顺序合成单个文本文件，便于直接贴给他人或喂给模型。
- **导出前脱敏开关**。默认开启，会对导出内容做与 `/log send` 一致的敏感信息掩码；需要原始内容时可在构建器里单次关闭，也可用新配置项 `mask_on_export` 改默认值。
- **预检**。点「预检」先算出文件数、压缩前体积、逐行裁剪条数和警告，确认没问题再点「开始导出」，避免误打包出几百 MB。
- **导出历史**。列出 `data/exports/` 下已生成的包，可重新下载、单个删除或一键清空。下载链接为一次性 token（10 分钟过期，最多同时保留 8 个），过期返回 410。
- **其它页签的快捷入口**。日志文件页「导出所选」、侧栏「导出此分类」、全局搜索页「导出结果」、实时日志页「导出视图」。前两个会跳到导出页并带好范围与预检结果，后两个直接在浏览器本地生成文本文件。
- **`/log export` 支持时间窗**。`/log export 3` 为最近 3 天，`/log export 2026-08-01 2026-08-20` 为指定区间，`/log export` 仍为全部。
- **4 个新配置项**。`mask_on_export`（导出是否脱敏，默认开）、`export_retention_days`（导出包保留天数，默认 7）、`export_max_total_mb`（导出目录体积上限，默认 512）、`export_max_files`（导出包数量上限，默认 20）。
- **32 个新测试**（`tests/test_export.py` 27 个 + `tests/test_console_assets.py` 5 个）。覆盖时间窗裁剪、参数解析与拒绝、级别与关键词过滤、两条脱敏路径、合并输出、预设与选择越界、预检计数、历史增删、下载 token 的一次性与过期、导出目录清理策略，以及导出面板的 DOM / 样式 / 文案完整性。

### 变更

- 总览页的「打包下载」卡整块移入导出页签。此前它被挤在总览最下方，指标卡一多就滚出可视区，实际上很难发现。
- 构建器任一表单项变动（范围、级别、预设、插件、格式、天数、起止日期、脱敏、关键词）都会立即失效上一次的预检结果与下载 token，预览区不再显示与当前表单不符的陈旧数字。
- 导出内核统一为「选择 → 渲染」两段：`plan_for()` 只负责算出要带哪些文件与哪些行，`build_export()` 只负责落盘，ZIP 与合并 TXT 共用同一套筛选逻辑，命令行与 WebUI 也走同一条路径。

### 修复

- **导出的 ZIP 不脱敏**。`_write_zip()` 少接了 masker 参数，导致无论配置如何，打包下载拿到的都是未掩码的原始日志，而同一份内容通过 `/log send` 发出去时是脱敏的。现在两条路径行为一致。
- **`data/exports/` 永不清理**。清理器只认 `.log` / `.log.gz` 等日志后缀，历次导出包会一直堆积直到手动删除。现在导出目录纳入自动清理，按保留天数、总体积、文件数三个上限回收，并且永远至少保留最新的 1 个包。
- **总览页导出入口被挤出可视区**。改为独立页签后不再依赖总览页的滚动位置。

## [2.2.0] - 2026-08-27

本次发布重写 WebUI，并把命令主入口从 `/logvault` 改为更短的 `/log`。

### 新增

- **WebUI 全面重置**。日志中心重做为单页控制台，分五个页签：运行总览、实时日志、日志文件、全局搜索、采集诊断。顶部为标题与操作区，底部为常驻状态栏，主体区域各自独立滚动，页面本身不再产生横向或纵向溢出。
- **6 套皮肤**。跟随 Dashboard（自动明暗）、深空控制台、明昼、**玻璃荧光**（半透明毛玻璃 + 荧光描边 + 背景光斑）、赛博霓虹、终端绿。皮肤通过 `html[data-skin]` 上的 CSS 变量驱动，切换不重载页面，选择记入 localStorage。
- **紧凑 / 宽松密度切换**。一键调整行高、字号与间距，同样持久化。
- **运行总览页**。指标卡（文件数、占用、压缩数、来源数、插件 logger 数、转发条数）、采集链路卡、来源清单卡、分类占用占比条、打包下载卡，以及可折叠的告警 banner。
- **实时日志页**。2 秒增量 tail，支持级别过滤、来源标签过滤、关键词过滤、暂停跟随、复制、清屏；渲染上限 2000 行，底层保留 6000 行环形缓冲，长时间跟随不会拖慢页面。
- **可搜索的「跟随文件」选择器**。实时日志页的文件下拉改为组合框：支持空格分词的多关键词过滤（同时匹配相对路径、文件名、分类名与来源名），结果按来源分组、命中片段高亮，可用 ↑ ↓ 选择、回车确认、Esc 取消。占位尺寸与原下拉一致，工具栏布局不变；原生 `<select>` 仍作为唯一状态源保留，其它逻辑与事件不受影响。
- **全局搜索页**。跨来源关键词搜索，结果按 `来源 / 相对路径 : 行号` 定位，可直接跳转到对应文件的抽屉查看器。
- **采集诊断页**。展示当前捕获模式、写入级别与上游生效级别、已挂载 logger、已注册接口、转发与丢弃计数，并把常见成因整理为排查建议列表。
- **抽屉式日志查看器**。从文件列表右侧滑出，支持跟随、级别与关键词过滤、复制、下载，关闭后自动停止轮询。
- **两级来源树**。先按来源（当前数据目录 / 只读历史目录 / 共享日志目录）分组，再按分类展开，插件日志单独成组并保留独立计数与占用。
- **14 个新测试**（`tests/test_console_assets.py`）。校验命令组与别名、帮助文案、`app.js` 引用的元素 id 在 HTML 中都存在、6 套皮肤样式齐全且玻璃皮肤确有毛玻璃与荧光声明、两种密度都有样式、页签与面板集合一致、品牌图标为主题中立的矢量图标、跟随文件选择器具备完整的组合框结构与函数、组合框不改变工具栏字段数量、中英文案完全对齐。

### 变更

- **命令主入口改为 `/log`**。`/log status`、`/log search`、`/log export`、`/log clean`、`/log help`、`/log send ...` 为推荐写法；`/logvault` 与 `/logplus` 保留为兼容别名，行为完全一致，已有用法不受影响。
- **品牌标记改为线性图标**。左上角原先的 `LV` 字母块换成描边式日志文件图标，`stroke="currentColor"` 继承当前皮肤的强调色，霓虹类皮肤（玻璃荧光 / 赛博霓虹 / 终端绿）额外附一层发光，6 套皮肤下观感一致。
- **i18n 重建**。页面文案抽为 168 个 key，`zh-CN.json` 与 `en-US.json` 完全对齐，缺失时回落到内置默认文案。
- 帮助输出与 `metadata.yaml` 描述同步更新为新的命令写法与 WebUI 能力。

### 修复

- 窗口变窄时页面出现横向滚动条、表格被挤出可视区。现在关键容器统一 `min-width: 0`，表格改为固定布局，并在 1080 / 900 / 680 三个断点重排。
- 超长告警文本把整块布局撑破。改为可折叠 banner，文本按任意字符换行。
- 侧栏分类名过长时被硬截断且无法辨认。改为省略号 + 完整名称悬浮提示。
- 内容查看区在窄屏下被侧栏挤压到几乎不可读。查看器改为抽屉式浮层，不再与列表争抢宽度。
- 大文件与长时间跟随导致的渲染卡顿。改为增量追加 + 渲染行数上限 + 环形缓冲。

## [2.1.0] - 2026-08-27

本次发布解决三个核心问题：日志采集不完整、导出天数参数失效、缺少可视化管理入口。

### 新增

- **loguru 全量捕获**（`core/loguru_capture.py`）。新增 `capture_mode`，默认 `auto` 直接向 loguru 注册 sink，把记录转换成 `logging.LogRecord` 后交给分流器，使核心、插件与第三方库的日志走同一条通路；注册失败自动回退旧的 `logging` 模式，两种模式互斥不会重复写入。新增 `capture_third_party` 控制兼容模式下是否额外挂载 root logger。
- **`force_source_debug`**。主动把 `astrbot` 与所有 `astrbot.plugin.*` logger 的级别降到 `log_level`。AstrBot 会把控制台级别同步到这些 logger，级别过高时低级别记录在到达任何 Handler 之前就已被丢弃，这是日志缺失最常见的原因。
- **启动期日志回填**。新增 `backfill_startup_logs` 与 `backfill_limit`，从 AstrBot 控制台日志缓存回放插件加载完成之前的记录，以 `.bootstrap_state.json` 保存时间水位，重启后不会重复回填。
- **`include_trace_log`**。按需包含 AstrBot trace 级链路日志，默认关闭。
- **捕获诊断**。`/logvault status` 追加一段诊断输出：当前捕获模式、写入级别、`astrbot` 生效级别、已转发与丢弃条数、回填条数、插件 logger 数量、WebUI 接口数量，以及配置冲突时的警告。
- **`slice_by_record_time`**（默认开启）。按每行日志自身的时间戳裁剪导出范围，取代原先按文件修改时间整份打包的做法。
- **WebUI 日志中心**。新增 `core/web_api.py`（`overview` / `files` / `content` / `tail` / `search` / `download` / `bundle` / `delete` / `clean` / `capture` 共 10 个 Dashboard 接口，导入采用三层回退，AstrBot 版本不支持插件页面时安静跳过）与 `pages/logs/` 页面：分类树、级别过滤、脱敏查看、2 秒增量实时跟随、跨来源搜索、单文件下载、按天打包、批量删除、手动清理。
- **界面多语言**。新增 `.astrbot-plugin/i18n/zh-CN.json` 与 `en-US.json`，跟随 Dashboard 语言设置。
- **`clean_interval_minutes`**。清理任务执行间隔改为可配置，默认 60 分钟，范围 1 分钟至 7 天，非法值回落默认值。
- **统一日志行格式**。新增 `LogVaultFormatter` 与 `logvault_tag` 字段，日志行携带来源标签，便于 WebUI 与导出侧解析。
- **测试** `tests/test_capture_and_webui.py`。覆盖按记录时间切片（含压缩文件的两种语义）、loguru 记录转换与 trace 门槛、回填幂等性、WebUI 分类与文件列表、级别过滤与脱敏、增量跟随与轮换重置、路径穿越拒绝与删除保护、清理间隔计算。测试总数由 17 增至 40。

### 修复

- **`send` / `export` 的天数参数被忽略，总是打包全部日志。** 两个独立原因：命令参数原本声明为 `int` 并带默认值，AstrBot 的参数解析会对其提前做 `int()` 转换，与 `send plugin <插件名> <天数>` 的位置参数组合冲突，天数没有落到处理函数；同时旧逻辑按文件修改时间筛选整份文件，而 `all.log` 是持续写入的活动文件，修改时间永远是最新，导致全部历史内容被打包。现在所有参数一律声明为 `str` 并由插件自行解析，`send all 3` 简写与三段式写法都能正确识别，非法天数返回插件自己的提示而不是解析器异常。
- **压缩归档在按天导出时的处理。** 最早一条记录已在范围内时原样保留 `.gz`；跨越时间边界时解压并重写为去掉 `.gz` 后缀的明文；裁剪后为空的文件整份跳过且不计入文件数；没有可解析时间戳的文件整份包含。
- **`send plugin` 的插件名解析。** 新增 `_resolve_installed_plugin`，把别名映射到规范插件 ID 并缓存 30 秒，减少重复扫描注册表。

### 变更

- README 重写：移除版本叙事，改为纯功能与配置文档，变更记录移入本文件。
- `metadata.yaml` 描述更新，反映 loguru 捕获与 WebUI 能力。
- `requirements.txt` 保持为空，插件仍不依赖任何第三方包。

## [2.0.3] - 2026-08-18

### 修复

- 捕获 AstrBot 4.27+ 的 `astrbot.plugin.<插件名>` 专属 logger，不再依赖旧版本的全局 logger 传播行为。
- 共享日志回退筛选生成的副本同样经过敏感信息脱敏。

### 文档

- 说明 AstrBot 的 logger 路由方式，以及为什么旧版导出中只能看到插件自己的日志。

## [2.0.2] - 2026-08-18

### 修复

- 按插件 ID 路由 AstrBot 后台日志，避免 `send plugin` 打包整份 Core 日志。
- Handler 改用富化后的 record 进行路由，分类结果与实际来源一致。

## [2.0.1] - 2026-08-18

### 修复

- 从 AstrBot 插件注册表与 `data/plugins` 发现已安装插件，不再把「尚未产生专属日志」误报为「插件不存在」。

## [2.0.0] - 2026-08-18

首个 LogVault 版本，从 [lxfight/astrbot_plugin_logplus](https://github.com/lxfight/astrbot_plugin_logplus) fork，改用独立的插件 ID 与数据目录，不与上游混用。

### 修复

- **Linux 上活动日志丢失。** 旧版清理任务可能压缩或删除仍被 `FileHandler` 打开的当前 `plugin.log`；Linux 允许删除已打开的文件，后续写入会进入目录中不可见的 inode。现在只处理已经轮换并关闭的文件，绝不删除活动日志。
- **插件归属识别。** 修复 Windows、Linux 与混合路径分隔符下的判定，插件日志不再被错误归入 Core。
- 压缩日志保留原始修改时间，按天筛选不会把旧内容误判为新日志。

### 新增

- 按插件与天数发送：`send plugin <插件名> [天数]`。
- 只读兼容旧版 `astrbot_plugin_logplus` 数据目录，以及 `legacy_data_dirs`、`host_log_dirs` 两个额外来源配置。
- 导出与搜索移入后台线程，减少大日志文件阻塞消息处理。

[2.1.0]: https://github.com/Whereis-Alice/astrbot_plugin_logvault/compare/610c328...7fee311
[2.0.3]: https://github.com/Whereis-Alice/astrbot_plugin_logvault/compare/a1c23e5...610c328
[2.0.2]: https://github.com/Whereis-Alice/astrbot_plugin_logvault/compare/bd86a34...a1c23e5
[2.0.1]: https://github.com/Whereis-Alice/astrbot_plugin_logvault/compare/aee669d...bd86a34
[2.0.0]: https://github.com/Whereis-Alice/astrbot_plugin_logvault/commit/aee669d
